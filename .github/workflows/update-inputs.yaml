# This file was autogenerated by actions.nix. Do not edit it manually.
# To make changes, edit the workflow definition in your flake's actions-nix configuration
# (typically under flake.actions-nix.workflows.".github/workflows/update-inputs.yaml") and run:
#   nix run .#render-workflows
# Or commit to trigger the pre-commit hook if enabled.
jobs:
  update-inputs:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8
      with:
        token: ${{ secrets.PAT }}
    - uses: cachix/install-nix-action@v24
      with:
        extra-conf: |-
          accept-flake-config = true
          always-allow-substitutes = true
          builders-use-substitutes = true
          max-jobs = auto
    - name: Update flake inputs
      uses: mic92/update-flake-inputs@main
      with:
        auto-merge: false
        commit-message: 'chore(flake): update inputs'
        github-token: ${{ secrets.PAT }}
        pr-labels: dependencies,nix
        pr-title: 'chore(flake): update inputs'
    timeout-minutes: 60
name: update-flake-inputs
'on':
  schedule:
  - cron: 0 2 * * 1
  workflow_dispatch: {}
permissions:
  contents: write
  pull-requests: write
